import { BinaryTreeNode } from "./binaryTree";

export const insertSort = <T>(
    rootNode: BinaryTreeNode<T>,
    insertedNode: BinaryTreeNode<T>,
    sort: (node: BinaryTreeNode<T>, insertedNode: BinaryTreeNode<T>) => boolean,
) => {
    
}